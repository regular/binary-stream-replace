{
  "name": "binary-stream-replace",
  "version": "1.0.0",
  "description": "efficiently replace a sequence of bytes in a binary or text stream with a different sequence of bytes. Can be restricted to first n occurrences only.",
  "main": "index.js",
  "scripts": {
    "test": "tape test/*.js"
  },
  "keywords": [
    "stream",
    "binary",
    "browserify",
    "replace"
  ],
  "author": "Jan Bölsche <jan@lagomorph.de>",
  "license": "ISC",
  "devDependencies": {
    "tape": "^3.2.0",
    "concat-stream": "^1.4.7"
  },
  "dependencies": {
    "through": "^2.3.6",
    "debug": "^2.1.1"
  }
}
